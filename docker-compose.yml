services:
  genpot:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
